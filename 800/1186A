#include <iostream>
#include <vector>
#include <string>
 
using namespace std;
 
int main()
{
    // your code goes here
    int n, m, k;
    cin >> n >> m >> k;
    if((m>=n) && (k>=n))
        cout << "yes";
    else
        cout << "no";
    // your code goes here
    return 0;
}